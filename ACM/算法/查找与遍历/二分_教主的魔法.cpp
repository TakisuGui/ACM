#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

ll n,q;
const int N=1e6+10;
ll a[N];

int main()
{
    cin>>n>>q;

    while(q--)
    {
        char c; cin>>c;
        if(c=='M')
        {
            int l,r,w;
            for(int i=l;i<=r;i++) a[i]+=w;
        }
        else
        {
            int l,r,c;
            
        }
    }

}